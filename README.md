
## Installation
```bash
git clone https://github.com/lovecoding-git/hatwizard.com

composer install
cp -r .env.example .env

php artisan migrate && php artisan key:generate

npm install && npm run dev
```
